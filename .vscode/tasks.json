{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Ruff Check Current File",
        "type": "shell",
        "command": "docker compose exec -T app ruff check ${relativeFile} --fix",
      }
    ]
  }